.venv/
__pycache__/
.claude/settings.local.json
# raw exports (~110 MB) stay out of git; copy DATA/*.csv back manually if you
# need to re-run prep.py — the committed data.js is enough to run the site
DATA/*.csv
~$*
# Local copies of assignment briefs, example submissions, and grading feedback.
project_instructions_and_examples/
